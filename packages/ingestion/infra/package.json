{
  "name": "@lantern/infra",
  "version": "0.0.0",
  "private": true,
  "description": "CDK stack: API Gateway, ingestion + rollup Lambdas, DynamoDB, EventBridge schedule",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.162.0",
    "constructs": "^10.4.0"
  },
  "devDependencies": {
    "aws-cdk": "^2.162.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0",
    "@types/node": "^22.7.0"
  }
}
