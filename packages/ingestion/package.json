{
  "name": "@lantern/ingestion",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AWS Lambda ingestion handler + infra-as-code (API Gateway, DynamoDB)",
  "main": "src/handler.ts",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --minify --platform=node --target=node20 --outfile=dist/handler.js --external:@aws-sdk/* && esbuild src/rollup-handler.ts --bundle --minify --platform=node --target=node20 --outfile=dist/rollup-handler.js --external:@aws-sdk/* && esbuild src/session-meta-handler.ts --bundle --minify --platform=node --target=node20 --outfile=dist/session-meta-handler.js --external:@aws-sdk/*",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.677.0",
    "@aws-sdk/lib-dynamodb": "^3.677.0",
    "@lantern/shared": "*"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.7.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
