# @lantern/ingestion

AWS Lambda handlers behind API Gateway (HTTP API), writing to DynamoDB.

Infra-as-code choice: leaning AWS CDK (TypeScript) to keep one language across the
whole repo — not committed yet, open to SAM/Terraform if a reason comes up.

`infra/` — CDK/SAM stack definitions (empty, not started).
`src/` — Lambda handler code (empty, not started).

No implementation yet.
