# @lantern/shared

Shared TypeScript types for the event schema (pageview events, session-recording
metadata) so the tracker, ingestion, and dashboard packages don't drift out of sync.

No implementation yet.
